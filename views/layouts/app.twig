<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Binsta</title>
    
    <!-- Bootstrap stylesheet, script and icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    
    <!-- Highlighting theme -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.12.0/styles/atelier-heath-dark.min.css"/>

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/stylesheets/style.css">
    
</head>

<header class="shadow-sm fixed-top">
    <!-- Navigation bar -->
    <nav class="navbar navbar-expand-sm py-0" style="background-color: #ffffff;">
        <div class="container-fluid d-flex justify-content-between position-relative">

            <!-- Navbar logo -->
            <a class="navbar-brand ms-2 my-2" href="/">
              <p class="m-0 fw-bold">Binsta</p>
            </a>

            <!-- Seachbar -->
            { if logged_in == 1 }
                <div class="collapse navbar-collapse w-auto flex-grow-0 align-items-center position-absolute top-50 start-50 translate-middle" id="navbarNavAltMarkup">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <form action="/user/search" method="POST">
                                <label hidden for="search-user" class="form-label">Search user</label>
                                <input type="text" class="form-control" style="background-color: rgb(245, 245, 245);" id="search-user" name="search-user" placeholder="Search...">
                            </form>
                        </li>
                    </ul>
                </div>
            { endif }

            <!-- Nav buttons -->
            <div class="collapse navbar-collapse flex-grow-0 me-0" id="navbarNavAltMarkup">
                <ul class="navbar-nav">
                    <li class="nav-item py-2 p-0">
                        <a href="/" class="link-dark"><i class="bi bi-house-fill fs-3 mx-3"></i></a>
                    </li>
                    <li class="nav-item py-2 p-0">
                        <a href="/post/create" class="link-dark"><i class="bi bi-plus-square fs-3 mx-3"></i></a>
                    </li>

                    <!-- Change visual if logged in or not -->
                    <li class="nav-item py-2">
                        { if logged_in == 1 }
                            <a href="/user/profile?id={{ active_user.id }}">
                                <img src="{{ active_user.image }}" alt="{{ active_user.username }} profile picture" style="border-radius: 50%; height: 2.5rem;" class="ms-3">
                            </a>
                        { else }
                            <div class="d-flex justify-content-end w-auto">
                                <a class="nav-link py-2" href="/user/login">Login</a>
                            </div>
                        { endif }
                    </li>
                </ul>
            </div>

            <!-- Toggler for collapsed navbar -->
            <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

        </div>
    </nav>
</header>

<body style="background-color: rgb(251, 251, 251); padding-top: 4rem;">
    <!-- Body to be filled in -->
    { block body }
    { endblock }
</body>
</html>